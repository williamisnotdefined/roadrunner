# AI Knowledge Base

Canonical AI guidance for Roadrunner lives under `ai`.

Generated route files are derived from `ai/registry.json` and canonical skill/reference files. Do not edit `.opencode`, `.cursor`, or `.github/instructions` route files manually.

## Commands

```bash
npm run ai:sync
npm run ai:check
```
